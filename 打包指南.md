# 打包成 exe 指南

本文档说明如何把「工作日志」项目打包成 Windows 单文件 exe，以及如何验证、分发和排查问题。

## 1. 前置准备

| 项目 | 要求 | 检查方法 |
|------|------|----------|
| Python | 3.x | `python --version` |
| PyInstaller | >= 6.0 | `python -m PyInstaller --version`（缺失会自动安装） |
| logo.ico | 必须存在 | 项目根目录已有；缺失时运行 `python make_logo.py` 生成 |

## 2. 方法一：双击打包（最简单）

1. 打开项目目录 `C:\Users\zhang\Desktop\Deepseek\Project\Work Log`
2. 双击 **`打包exe.bat`**
3. 等待 1~3 分钟，看到 `打包完成：...\工作日志.exe` 即成功
4. 项目根目录出现 **`工作日志.exe`**（约 10MB 单文件）

该脚本内部实际执行 `python build_exe.py`。

## 3. 方法二：命令行打包

```powershell
cd "C:\Users\zhang\Desktop\Deepseek\Project\Work Log"
python build_exe.py
```

`build_exe.py` 会自动完成 4 件事：
1. 检测 PyInstaller，未安装则 `pip install pyinstaller`
2. 检查 `logo.ico` 是否存在
3. 执行 PyInstaller 打包（`--onefile --windowed`）
4. 把 `dist\工作日志.exe` 复制到项目根目录

## 4. 方法三：手动执行 PyInstaller（进阶）

```powershell
python -m PyInstaller --noconfirm --clean --onefile --windowed --name 工作日志 --icon logo.ico --add-data "logo.ico;." --add-data "logo_64.png;." worklog.py
```

### 参数含义

| 参数 | 作用 |
|------|------|
| `--noconfirm` | 覆盖旧产物，不询问 |
| `--clean` | 清理缓存，避免残留导致异常 |
| `--onefile` | 打包成单个 exe |
| `--windowed` | 不弹出黑色命令行窗口（GUI 程序） |
| `--name 工作日志` | 生成的 exe 文件名 |
| `--icon logo.ico` | 设置 exe 图标 |
| `--add-data "logo.ico;."` | 把 logo 文件一起打进 exe（Windows 分隔符为 `;`，Linux/Mac 为 `:`） |
| `worklog.py` | 程序入口 |

手动方式打包后需自行复制产物到根目录：

```powershell
Copy-Item dist\工作日志.exe .
```

## 5. 打包后的验证

1. 双击 `工作日志.exe`，确认界面正常打开
2. 命令行自动验证：

```powershell
.\工作日志.exe --smoke   # 冒烟测试，退出码 0 表示正常
```

3. 或双击 **`运行测试.bat`**（单元测试 + exe 冒烟测试）

## 6. 分发说明

- `工作日志.exe` 是单文件，拷贝到任何 Windows 电脑双击即可运行，目标电脑无需安装 Python
- 运行记录保存在 exe 旁边的 `data/worklog.json`
- 周报导出到 exe 旁边的 `reports/` 目录
- 建议整个文件夹（exe + data + reports）一起分发，数据随带随走

## 7. 常见问题排查

| 现象 | 原因与解决 |
|------|-----------|
| 提示「不是内部或外部命令」 | Python 未加入 PATH，改用完整路径 `C:\Program Files\Python311\python.exe build_exe.py` |
| 打包很慢 | 首次打包需分析依赖，属正常；之后有缓存会快一些 |
| 杀毒软件误报 | PyInstaller 单文件 exe 常见误报，添加信任即可 |
| 目标电脑报缺 `VCRUNTIME140.dll` | 安装 Microsoft Visual C++ Redistributable |
| exe 打不开且无报错 | 命令行运行 `.\工作日志.exe --smoke` 查看退出码排查 |

## 8. 日常维护建议

- 改完代码重新打包：直接双击 `打包exe.bat` 即可
- `工作日志.spec` 是 PyInstaller 自动生成的配置文件，可删可留（`build_exe.py` 不使用它）
- 打包产物（`build/`、`dist/`、`*.exe`）已在 `.gitignore` 中排除，不会上传 GitHub
