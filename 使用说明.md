# 工作日志 使用说明

## 启动

- **打包版**：双击 **工作日志.exe**（单文件，可拷贝到任意 Windows 电脑，无需安装 Python）
- **源码版**：双击 启动工作日志.bat（或命令行执行 `python worklog.py`）

## 重新打包 exe

改完代码后重新打包：双击 **打包exe.bat**（首次会自动安装 PyInstaller，约 1~3 分钟），
产物为根目录的 工作日志.exe。打包后自检：`工作日志.exe --smoke`（退出码 0 即正常）。

## Logo

- 应用图标/周报页眉徽标：靛蓝渐变圆角方块 + 白色日志文档 + 绿色完成圆勾（“记录并完成工作”）
- 图标文件由 `make_logo.py` 生成（logo.ico / logo.png / logo_64.png），改配色或样式后执行 `python make_logo.py` 重新生成，再重新打包
- 周报正文顶部带有「✓ 工作日志 · 周报」品牌徽标（纯内联样式，粘贴到 Outlook 同样不变形）

## 日常使用

1. **首次运行**：弹出「周设置」对话框，可填写**汇报人**（会显示在周报标题下方，留空则不显示）、
   确认起始日期并勾选工作日（默认周一至周五），点确定。
   之后每周开头会自动提示设置新的一周；也可随时点右上角「周设置」修改。
   **新的一周建好时，上一周未收尾的事项会自动带入第一个工作日**，不用重新敲一遍。
2. **记录当日工作**：标头显示本周共几个工作日，次标头显示当前日期、周几、是第几个工作日。
   在「工作内容」栏填写事项，用「状态」下拉标注：未开始 / 进行中 / 已完成；
   遇到问题写在「难点备注」栏（会汇总进周报的“难点与问题”）。
   内容框支持多行：回车换行，内容多了自动增高，周报里换行也会保留。
3. **复制昨日**：点「⧉ 复制昨日」，把上一个工作日**未完成**的条目（含难点）搬到今天，
   已完成的不会带过来，与当天已有的同名条目也会自动跳过，不产生重复。
4. **完成一天**：勾选「今日记录已完成」，自动保存并跳到下一个工作日。
   标头会实时显示「已填写 X/Y 天」。
   常用快捷键：`Ctrl+S` 保存、`Ctrl+Enter` 添加一行、`Ctrl+D` 删除选中行。
5. **回看**：用「◀ 前一天 / 明天 ▶」切换日期，或点「📅 历史记录」按周浏览、双击跳转。
   历史记录里选中某日期可「删除该日记录」，选中周标题可「删除整周」（均有确认提示）。
6. **下周计划**：底部输入框填写，会写入周报第四部分；不填则自动草拟（承接本周进行中/未开始事项）。
7. **搜索**：点右上角「🔍 搜索」，输入关键词即**实时模糊搜索**全部历史与当前记录：
   - 匹配范围：工作内容、难点备注、状态、各周「下周计划」；
   - 模糊匹配：输入「联调」即可命中「支付接口联调」，大小写不敏感；
   - 多个关键词用空格分隔，须全部命中（如「支付 验签」）；
   - 结果按周从新到旧排列，当前周标注「（当前周）」，命中词黄色高亮；
   - 单击选中一行、双击或回车跳转到对应日期；Esc 关闭。

## 生成周报

所有工作日填写完成后，点 **「📤 生成周报（上传）」**：

- 有未完成的天会先弹确认；
- 预览纯文本可当场修改，点「💾 保存修改」会回写到记录。
  回写前会弹确认框，说明将覆盖几天、几条记录；
  **某一天的「1. 内容 —— 状态」格式若被改动导致无法解析，该天会自动跳过、保留原记录**，
  不会像以前那样被清空。回写前也会自动在 `data/backup/` 留一份快照。
- 导出方式四种：
  - **复制 HTML（粘贴到 Outlook）**：剪贴板里是带样式的表格，到 Outlook 邮件正文 Ctrl+V 即可，不变形（推荐）；
  - **导出 HTML 文件** / **浏览器预览**：保存到 `reports/` 目录，浏览器打开后也可复制到 Outlook；
  - **导出纯文本**：`.txt` 文件，100% 不变形；
  - **用 Outlook 打开邮件**：直接调起 Outlook 新建邮件并填入周报（需 `pip install pywin32`，且本机为配置好的经典版 Outlook；新版 Outlook 的 COM 受限，此按钮会提示改用「复制 HTML」）。

## 数据存储与找回

- 所有记录自动保存到 `data/worklog.json`（退出时也会保存），即“记忆功能”。
  写入采用「临时文件 + fsync + 原子替换」，断电或强杀进程都不会写出半截文件。
- 数据文件损坏时会自动备份为 `worklog.json.bad` 并重建；结构异常（被手工改坏、字段类型不对）
  会在读取时自动修复，不会导致程序打不开。
- **自动备份在 `data/backup/`**：每天首次保存前留一份当日快照，
  删除某日/整周、周报回写前也各留一份，最多保留 10 份（超出删最旧的）。
  误删了想找回：关掉程序，把 `data/backup/` 里对应时间的文件复制成 `data/worklog.json` 即可。

## 开发者：跑测试

改完代码后建议先跑测试再打包：

```
pytest -q                 # 纯逻辑单测（不需要界面，约 54 个用例）
python worklog.py --smoke # 冒烟测试：真起一次界面走完记录→周报→回写全流程
```
打包后的 exe 同样可自检：`工作日志.exe --smoke`（退出码 0 即正常）。

## 周报结构

1. 本周工作概述（工作日数、**推进事项数**、完成/进行中/未开始统计、难点条数）
   - 统计口径：同一件事连着记好几天，只算 **1 项**，括号里另外标注「共记录 N 条明细」，
     避免概述数字虚高。
2. 本周工作明细（按日表格：序号 / 工作内容 / 状态 / 难点备注，状态带颜色）
3. 难点与问题汇总
4. 下周计划

## 跨周事项关联

同一件事在不同周之间会自动关联（按内容一致识别，跨天/跨周注记只在**生成周报**时体现）：

- **正向**：A 周某项还是「进行中」，B 周（或更晚）标记了同内容的「已完成」——
  A 周的周报会把这项**计入已完成**，并在明细里注记「已于 2026.08.25 完成，下周」（隔 2 周则注「后 2 周」），
  下周计划的自动草拟也不再承接它；
- **反向**：B 周的「已完成」条目如果之前某周曾「进行中」过，明细里会注记
  「承接 2026.08.17，上周」（隔 2 周则注「前 2 周」），让读者知道这件事是从哪周接过来的。

所以周报是**随时间自我修正**的：本周结束时生成会显示「进行中」，等下周完成后回头重新生成，
这条就会自动变成「已于下周完成」并带上承接关系。跨周关联要求两周里填写的内容文字一致
（新建周自动承接、复制昨日都是原样复制，正常使用会自动满足）。
