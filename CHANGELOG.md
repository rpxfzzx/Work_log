# 更新记录

## 2026-09-08 代码分析整改：数据安全加固 + 对话框拆分 + 撤销栈

按代码评审意见做的一轮整改：

**数据安全**

- 单实例保护：同一数据目录只允许一个实例运行（Windows 互斥体，按数据目录命名），
  避免两个进程抢写 `worklog.json` 互相覆盖；
- 保存的临时文件按进程号命名，写前清理历史残留，进一步降低并发/崩溃残留风险；
- 损坏数据备份 `.bad` 改为带时间戳且保留最近 3 份（原来单槽会被第二次损坏覆盖）；
- 快照与 `.bad` 文件名在时间戳同名时追加序号（Windows 时钟分辨率约 15ms，同秒会撞名）；
- 调整已有周的工作日时，若被取消勾选的日期有记录，弹确认说明「记录将不再显示
  （仍保留在文件里）」；周报回写、删行/删日/删周、改工作日前推送**撤销快照**，
  新增 `Ctrl+Z` 撤销（最多 20 步）；
- `Ctrl+D` 未选中行时删最后一行改为先确认（有撤销兜底）；
- 周报草稿保存时记录数据指纹，之后记录被改过再打开草稿会提示选择「显示草稿 /
  按当前记录重新生成」，避免旧草稿静默覆盖新数据；
- 新增运行日志 `data/worklog.log`（轮转 2×512KB）：数据损坏、快照失败、剪贴板失败、
  未捕获异常都会落日志，未捕获异常同时弹窗提示。

**回写解析修正**

- 纯文本回写按「最后一个 ——」切分内容与状态（贪婪匹配），工作内容本身含破折号
  （如“修复 A——B 的缺陷”）不再被截断；
- 多行难点还原改为按「工作内容」匹配而不是按位置：在预览里删行/调序后，
  多行难点仍还原到原条目。

**结构优化**

- 拆出 `dialogs.py`：周设置 / 历史记录 / 搜索 / 常用工作清单 / 周报预览对话框
  全部移入，`worklog.py` 从 1454 行降到 956 行，主窗口只留薄包装；
- 对话框统一 `focus_force()`，保证 Esc / 回车等键盘操作生效；
- 启动定位逻辑（今天所在周视图选择）两处重复合并为 `_locate_view()`；
- 状态下拉配色从 report 常量读取，消除两处硬编码；
- 搜索输入 200ms 防抖后再全量重绘；历史记录对话框支持 Esc 关闭；
- PowerShell 剪贴板回退的临时文件按进程号命名；
- `工作日志.spec`（含本机绝对路径）移出版本管理并加入 .gitignore，打包统一走 build_exe.py。

**测试**

- 新增 7 个单测：.bad 多份保留与清理、残留 tmp 清理、内容含破折号回写、
  难点按内容还原（行序无关）、周数据指纹变化检测；当前 68 个单测全部通过，
  冒烟测试通过。

修改位置：

- `storage.py`：坏数据时间戳备份+保留份数、tmp 进程号+清理、`setup_logging`、`cleanup_stale_tmp`、`_clean_week` 保留 `report_draft_fp`
- `todo_list.py`：tmp 进程号 + 写前清理
- `report.py`：`_ITEM_RE` 贪婪匹配、`_restore_multiline_diff` 按内容还原、`week_fingerprint`、剪贴板失败日志、powershell tmp 进程号
- 新增 `dialogs.py`：五个对话框移入 + 孤儿工作日确认 + 草稿指纹检查 + 搜索防抖 + `focus_force`
- `worklog.py`：撤销栈（`_push_undo`/`undo`/`Ctrl+Z`）、删除确认、单实例互斥体、启动定位去重、颜色复用、日志与异常钩子
- `tests/test_storage.py` / `tests/test_report.py`：新增与更新用例
- `使用说明.md`：快捷键、单实例、日志、备份说明同步更新

## 2026-08-27 常用工作清单完善：自动建目录、补保存按钮、移除复制昨日

- 启动时自动创建 `todo_list-config/todo_list.json`（新增 `todo_list.ensure_file()`），
  运行 exe 即可看到配置目录，不用先手动建；
- 常用工作对话框补充「保存」（不关窗口）按钮，点右上角 ✕ 关闭同样保存，避免误以为没有保存入口；
- 主界面移除「⧉ 复制昨日」按钮（功能删除，其余按钮不变）；
- 新增 1 个单测（ensure_file），当前 62 个单测全部通过。

修改位置：

- `todo_list.py`：新增 `ensure_file()`
- `worklog.py`：`__init__()` 调用 `ensure_file()`；`open_todo_list()` 增加「保存」按钮与
  ✕ 保存；`_build_ui()` 移除「⧉ 复制昨日」按钮
- `tests/test_todo_list.py`：新增 `test_ensure_file_启动时自动创建目录与空清单`
- `README.md` / `使用说明.md`：同步更新

## 2026-08-27 新增「常用工作清单」：下拉快速录入

新增自定义日常常做工作模块：

- 右上角「📌 常用工作」按钮：管理清单（添加 / 删除 / 上移下移排序）；
- 录入当天工作时，点工作内容框右侧「▾」按钮（或右键内容框）从清单直接选择，
  不用每次手敲；手动输入依然保留，两种方式可混用；
- 清单保存在程序旁的 `todo_list-config/todo_list.json`（原子写、读取时自动修复结构），
  换电脑把整个目录拷走即可；
- 新增 5 个单测，当前 61 个单测全部通过。

修改位置：

- 新增 `todo_list.py`：清单读写（`load_items` / `save_items`，目录 `todo_list-config/`）
- `worklog.py`：
  - 工具栏新增「📌 常用工作」按钮（`_build_ui`）；
  - `add_row()`：工作内容框右侧新增「▾」下拉按钮，右键内容框同效；
  - 新增 `_show_todo_menu()` / `_insert_todo_item()` / `open_todo_list()` / `_move_todo_item()`
- 新增 `tests/test_todo_list.py`：5 个读写单测
- `README.md` / `使用说明.md`：日常使用部分新增「常用工作清单」说明

## 2026-08-27 打包修复：内置 pywin32（Outlook 按钮）

「用 Outlook 打开邮件」按钮在 exe 中报「未安装 pywin32」：打包环境此前未装 pywin32。
已安装 pywin32 312 并重新打包，exe 内置 `win32com`（含 `pythoncom312.dll` / `pywintypes312.dll`），
在装有经典版 Outlook 的电脑上可直接调起邮件；新版 Outlook（无 COM 注册）仍按提示改用「复制 HTML」。
另：`工作日志.spec` 中的绝对路径更新为当前目录。

## 2026-08-27 概述统计口径调整：同名「已完成」按条数计

「推进事项」中「已完成」的统计原按内容去重，同名的已完成条目跨天重复记录会被合并成一例。
现在：

- 同名「已完成」条目**按记录条数各算一项**（完成几次算几项），不再合并成一例；
- 「进行中 → 已完成」关联不变：进行中事项后续完成时并入完成，不另算；
- 「未开始 → 进行中 → 已完成」只算一例，计入已完成。

新增 2 个单测（同名已完成不合并、未开始到完成只算一例），冒烟测试补同名完成断言；当前 56 个单测全部通过。

修改位置：

- `report.py` `collect_stats()`（约 147~199 行）：unique 计数改为「已完成按条数计、未完成同名合并、
  完成后并入」，docstring 同步更新；`overview_sentence()` docstring 同步口径说明
- `worklog.py` `run_smoke()`（约 1241 行）：补同名已完成按条数计的冒烟断言
- `tests/test_report.py`：新增 `test_同名已完成条目按条数计不合并`、
  `test_未开始到进行再到完成只算一例`
- `README.md` / `使用说明.md`：周报结构「统计口径」说明更新

## 本次改进（依据代码评审结果）

### 严重：会丢数据的问题

**1. 周报「保存修改」不再静默清空当天记录**
`apply_plain_back()` 原来遇到日期行就建空列表，最后无条件覆盖。只要某天的
`1. 内容 —— 状态` 格式被改动（换成冒号、改了序号、加了注释），这天就被写成空列表并立即落盘，
原记录永久消失。现在：解析不出条目的日期**直接跳过、保留原数据**；
回写前弹确认框说明「将覆盖几天几条 / 哪几天会被跳过」，并自动在 `data/backup/` 留快照。
新增 `plain_back_summary()` 做只读影响评估供界面使用。

**2. 多行难点不再在原样回写时丢换行**
`build_plain()` 用 `_flat()` 把难点压成一行，回写无法还原——用户什么都不改、只点一次
「保存修改」，多行难点就变单行了。现在逐条比对：压平后与原值一致即视为「未改动」，
恢复原始多行文本；用户确实改了则以修改后的为准。

**3. 数据文件容错**
`load_data()` 原来只做 `setdefault`，遇到 `{"weeks": []}` 这类结构错误会在后续
`AttributeError` 崩溃。现在逐层校验类型并自动修复（非法状态归一为「未开始」、
非字典条目丢弃、非日期 key 剔除），加了 `version` 字段为以后迁移留出口。
`save_data()` 补 `flush + fsync`，避免断电得到空文件。
新增 `data/backup/`：每天首次保存前、删除某日/整周前、周报回写前各留一份快照，最多保留 10 份。

### 逻辑与平台

**4. 概述统计不再跨日重复计数**
同一件事连记三天原来算「3 项」。现在按内容去重后统计「推进事项」，
状态取跨天最高（已完成 > 进行中 > 已完成），条数另在括号里标注：

```
旧：本周共 5 个工作日，记录工作 7 项：已完成 4 项，进行中 0 项，未开始 3 项。
新：本周共 5 个工作日，推进事项 5 项：已完成 2 项，进行中 0 项，未开始 3 项（共记录 7 条明细）。
```

下周计划的自动草拟同样去重，同一事项不再重复列出。

**5. 剪贴板 Win32 调用修正**
`GlobalAlloc / GlobalLock / SetClipboardData` 补齐 `argtypes / restype`——
ctypes 默认按 `c_int` 处理返回值，64 位 Python 上句柄会被截断成 32 位，
`memmove` 可能写到非法地址（静默失败或崩溃）。同时修掉 `memmove` 多读 1 字节的越界，
并把 `EmptyClipboard()` 挪到内存准备成功之后——原来中途失败会把用户剪贴板清空。

**6. PowerShell 回退方案**
显式调用 `powershell.exe`（`-AsHtml` 仅 Windows PowerShell 5.1 提供，PowerShell 7 已移除该参数），
并在缺参数时给出可读的中文提示。

**7. 历史记录窗口不再吞掉未保存的编辑**
跳转/删除前强制 `collect_and_save()`；窗口改为模态（`grab_set`），避免开出多个各自改数据。
删除操作前自动留快照。

**8. 跨平台**
`os.startfile` 换成 `webbrowser.open`（前者只存在于 Windows，其他平台是 `AttributeError`，
原来只捕获了 `OSError`）。

### 工程化

- 清理死代码：`storage.find_week_key()`（无调用）、`stats["with_items"]`（算了不用）。
- `settings.reporter` 从「存了从不用」改为真正启用：周设置里可填汇报人，显示在周报标题下方。
- 新增 `storage.peek_day()`：刷新界面和统计改为只读，不再往数据里塞空的日期结构。
- `工作日志.spec` 的绝对路径 `C:/Users/zhang/...` 改为基于 `SPECPATH`，换机器可直接打包。
- 新增 `.gitignore`（排除 `build/`、`__pycache__/`、`data/`、`reports/`）和 `requirements.txt`。
- 新增 `tests/`：42 个纯逻辑单测，不依赖 Tk，可进 CI；覆盖上面每一条修复。
  `run_smoke` 也补了复制昨日、回写保护、多行难点、结构容错的断言。
- 新增 `运行测试.bat`：一键跑 `pytest` + 冒烟。

### 体验与功能

- **高 DPI 适配**：启动时声明 per-monitor DPI aware，125%/150% 缩放下界面不再发虚。
- **「⧉ 复制昨日」按钮**：把上一个工作日未完成的条目（含难点）搬到今天，
  已完成的不带、与当天同名条目自动跳过。
- **新周自动承接上周未完成事项**：建新周时写入第一个工作日（该日已有记录则不动）。
- **快捷键**：`Ctrl+Enter` 添加一行、`Ctrl+D` 删除选中行（原有 `Ctrl+S` 保留）。

## 2026-08-23 搜索功能（历史 + 现状模糊搜索）

右上角新增「🔍 搜索」按钮，打开搜索对话框实时模糊搜索全部记录：

- 匹配范围：工作内容 / 难点备注 / 状态 / 各周「下周计划」；纯逻辑在 `report.search_items()`（可单测）；
- 模糊匹配：子串匹配、大小写不敏感；多个关键词空格分隔须全部命中（AND）；
- 结果按周从新到旧（当前记录在前），当前周标注「（当前周）」，命中关键词黄色高亮；
- 单击选中一行，双击或回车跳转到对应日期（回车的绑定在对话框窗口级，焦点在任意位置都可用）；
- 对话框打开后 `focus_force` 强制焦点到输入框——Tk 在有 modal grab 时合成/实际键盘事件
  都按**焦点窗口**投递，焦点不在输入框上事件会被丢弃，曾导致「打开后直接打字无效」；
- 新增 6 个单测（子串/多词 AND/难点状态/大小写/下周计划/排序/空关键词），当前 54 个单测全部通过。

## 2026-08-23 跨周事项关联

原关联只覆盖「同周内」：某日进行中 → 同周之后某天已完成。现在延伸到**跨周**（`collect_stats` 等
接收 `data` 参数，按内容一致自动识别）：

- **正向**：A 周「进行中」→ B 周或更晚「已完成」：A 周周报计入已完成，明细注记
  「已于 X 完成，下周 / 后 N 周」，自动草拟的下周计划不再承接；
- **反向**：本周「已完成」条目若之前周曾「进行中」，明细注记「承接 X，上周 / 前 N 周」；
- 同周内完成仍优先按同周日期注记；不传 `data` 时行为与旧版完全一致（兼容单测与旧调用）。
- 新增 `week_gap_label()` 计算周间隔；`unfinished_items` / `next_week_plan` /
  `overview_sentence` / `plain_back_summary` / `apply_plain_back` 全部透传 `data`，
  界面（新建周承接、周报预览与回写）已同步传入。
- 新增 6 个单测（正向/反向/最早完成与开始/同周优先/不传 data 兼容/周间隔标签），
  冒烟测试补跨周断言；当前 48 个单测全部通过。

## 尚未处理（建议后续）

- `worklog.py` 仍是 900+ 行单文件、`WorkLogApp` 单类约 700 行。
  建议把 `ReportDialog`、周设置、历史记录三个对话框拆到 `ui_dialogs.py`，主类只留状态管理。
  这次没动是因为纯结构重构风险高于收益，且无法在本机跑 Windows 端真实回归。
- 无日志系统：`except Exception` 仍会吞掉剪贴板/Outlook 的失败原因，
  建议接 `logging` 写到 `data/worklog.log`。
- 撤销删除（现在只有确认框，没有后悔药，仅能从 `data/backup/` 手工恢复）。
- 条目拖动排序、月报/季度汇总、工时或项目标签字段。
- 内容框超过 6 行时无视觉提示。
